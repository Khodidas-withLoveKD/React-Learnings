export const parentPaths = {
	SCRIMBA: 'scrimba-course/',
	HOOKS: 'hooks/',
	WORK_EXPERIMENTS: '/work-experiments/',
	REACT_BEST_PRACTICES: '/react-best-practices',
}

export const scrimbaCoursePaths = {
	DIGITAL_BUSSINESS_CARD: 'digital-business-card',
}

export const hooksPaths = {
	USE_STATE_HOOK: parentPaths.HOOKS + 'use-state-hook',
	USE_EFFECT_HOOK: parentPaths.HOOKS + 'use-effect-hook',
	USE_REF_HOOK: parentPaths.HOOKS + 'use-ref-hook',
}

export const workExperimentPaths = {
	COLOR_CODE_RETENTION: parentPaths.WORK_EXPERIMENTS + 'color-code-retention',
	XLSX_CSV_EDITOR: parentPaths.WORK_EXPERIMENTS + 'xlsx-csv-editor',
}

export const reactBestPracticesPaths = {
	PAGE_AND_PRESENTATIONAL_COMPONENTS:
		parentPaths.REACT_BEST_PRACTICES + '/pages-and-presentational-components',
}
