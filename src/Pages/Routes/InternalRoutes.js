import {pathNames} from './homeRoutes'

export const scrimbaCourseRoutes = {
  DIGITAL_BUSSINESS_CARD: pathNames.SCRIMBA + 'digital-business-card'
}

export const hooksRoutes = {
  USE_STATE_HOOK: pathNames.HOOKS + 'use-state-hook',
  USE_EFFECT_HOOK: pathNames.HOOKS + 'use-effect-hook',
  USE_REF_HOOK: pathNames.HOOKS + 'use-ref-hook' 
}